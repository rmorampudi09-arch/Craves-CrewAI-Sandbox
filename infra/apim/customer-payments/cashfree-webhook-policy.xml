<policies>
  <inbound>
    <!-- Deliberately omit <base /> here. The Cashfree webhook is authenticated by its
         provider HMAC signature, not by the customer Bearer-token policy inherited
         by normal payment operations. The raw request body is never parsed or
         rewritten at APIM so Integration Service can verify Cashfree's signature. -->
    <choose>
      <when condition="@(!context.Request.Headers.ContainsKey(&quot;x-webhook-timestamp&quot;) || !context.Request.Headers.ContainsKey(&quot;x-webhook-signature&quot;) || !context.Request.Headers.ContainsKey(&quot;x-webhook-version&quot;))">
        <return-response>
          <set-status code="400" reason="Bad Request" />
          <set-header name="Content-Type" exists-action="override"><value>application/json</value></set-header>
          <set-body>{"error":"CASHFREE_WEBHOOK_HEADERS_REQUIRED"}</set-body>
        </return-response>
      </when>
    </choose>
    <set-backend-service base-url="__PAYMENT_BACKEND_URL__" />
  </inbound>
  <backend><base /></backend>
  <outbound>
    <set-header name="Cache-Control" exists-action="override"><value>no-store, no-cache, must-revalidate</value></set-header>
    <set-header name="Pragma" exists-action="override"><value>no-cache</value></set-header>
    <set-header name="X-Content-Type-Options" exists-action="override"><value>nosniff</value></set-header>
  </outbound>
  <on-error />
</policies>
