<policies>
  <inbound>
    <base />
    <choose>
      <when condition="@(string.IsNullOrWhiteSpace(context.Request.Headers.GetValueOrDefault(&quot;Authorization&quot;, &quot;&quot;)) || !context.Request.Headers.GetValueOrDefault(&quot;Authorization&quot;, &quot;&quot;).StartsWith(&quot;Bearer &quot;, StringComparison.OrdinalIgnoreCase))">
        <return-response><set-status code="401" reason="Unauthorized" /><set-header name="Content-Type" exists-action="override"><value>application/json</value></set-header><set-body>{"code":"AUTHENTICATION_REQUIRED"}</set-body></return-response>
      </when>
    </choose>
    <set-backend-service base-url="__CHEF_ORDERS_BACKEND_URL__" />
  </inbound>
  <backend><base /></backend>
  <outbound><base /><set-header name="Cache-Control" exists-action="override"><value>no-store, no-cache, must-revalidate</value></set-header></outbound>
  <on-error><base /></on-error>
</policies>
