# auth

Standalone auth screens if you move off the AuthModal (e.g. /login, /register, /forgot-password pages).

This folder is currently empty — add a page component here, then wire it up with a new file in `src/routes/`.
