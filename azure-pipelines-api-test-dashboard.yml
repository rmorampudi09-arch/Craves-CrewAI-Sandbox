trigger: none
pr: none

parameters:
  - name: resourceGroup
    type: string
    default: rg-craves-prodlow-centralindia
  - name: acrName
    type: string
    default: cravesprodlowacr82121
  - name: containerAppName
    type: string
    default: ca-craves-web-prodlow
  - name: imageTag
    type: string
    default: $(Build.BuildId)

variables:
  modulePath: apps/api-test-dashboard
  imageRepository: craves/api-test-dashboard
  azureServiceConnection: $(AZURE_SERVICE_CONNECTION)
  apimBaseUrl: https://api.craves.in

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self

  - task: AzureCLI@2
    displayName: Build and push API test dashboard image
    inputs:
      azureSubscription: $(azureServiceConnection)
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        set -euo pipefail

        ACR_LOGIN_SERVER=$(az acr show \
          --name "${{ parameters.acrName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --query loginServer \
          -o tsv)

        IMAGE="${ACR_LOGIN_SERVER}/$(imageRepository):${{ parameters.imageTag }}"
        echo "Building ${IMAGE}"

        az acr login --name "${{ parameters.acrName }}"

        docker build \
          -f "$(modulePath)/Dockerfile" \
          --build-arg NEXT_PUBLIC_FIREBASE_API_KEY="$(NEXT_PUBLIC_FIREBASE_API_KEY)" \
          --build-arg NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="$(NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN)" \
          --build-arg NEXT_PUBLIC_FIREBASE_PROJECT_ID="$(NEXT_PUBLIC_FIREBASE_PROJECT_ID)" \
          --build-arg NEXT_PUBLIC_FIREBASE_APP_ID="$(NEXT_PUBLIC_FIREBASE_APP_ID)" \
          --build-arg NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="$(NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET)" \
          --build-arg NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="$(NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID)" \
          --build-arg NEXT_PUBLIC_APIM_BASE_URL="$(apimBaseUrl)" \
          -t "${IMAGE}" \
          "$(modulePath)"

        docker push "${IMAGE}"

  - task: AzureCLI@2
    displayName: Deploy API test dashboard to existing web Container App
    inputs:
      azureSubscription: $(azureServiceConnection)
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        set -euo pipefail

        ACR_LOGIN_SERVER=$(az acr show \
          --name "${{ parameters.acrName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --query loginServer \
          -o tsv)

        IMAGE="${ACR_LOGIN_SERVER}/$(imageRepository):${{ parameters.imageTag }}"
        echo "Deploying ${IMAGE} to ${{ parameters.containerAppName }}"

        az containerapp ingress update \
          --name "${{ parameters.containerAppName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --type external \
          --target-port 3000 \
          --transport auto \
          -o table

        az containerapp update \
          --name "${{ parameters.containerAppName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --image "${IMAGE}" \
          --set-env-vars \
            NODE_ENV=production \
            PORT=3000 \
            NEXT_PUBLIC_FIREBASE_API_KEY="$(NEXT_PUBLIC_FIREBASE_API_KEY)" \
            NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="$(NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN)" \
            NEXT_PUBLIC_FIREBASE_PROJECT_ID="$(NEXT_PUBLIC_FIREBASE_PROJECT_ID)" \
            NEXT_PUBLIC_FIREBASE_APP_ID="$(NEXT_PUBLIC_FIREBASE_APP_ID)" \
            NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="$(NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET)" \
            NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="$(NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID)" \
            NEXT_PUBLIC_APIM_BASE_URL="$(apimBaseUrl)" \
          -o table

        WEB_FQDN=$(az containerapp ingress show \
          --name "${{ parameters.containerAppName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --query fqdn \
          -o tsv)

        echo ""
        echo "Craves API Test Dashboard URL: https://${WEB_FQDN}"
        echo "Firebase Authorized domain must include: ${WEB_FQDN}"

  - task: AzureCLI@2
    displayName: Show latest web app logs
    condition: always()
    inputs:
      azureSubscription: $(azureServiceConnection)
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        az containerapp logs show \
          --name "${{ parameters.containerAppName }}" \
          --resource-group "${{ parameters.resourceGroup }}" \
          --tail 120 || true
