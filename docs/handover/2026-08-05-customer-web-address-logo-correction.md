# Correction

The earlier logo patch modified `components/layout/Logo.tsx`, but the deployed public landing page renders `CravesLogo` directly from `HeroSection.tsx`. This repair changes the actual rendered component and its production asset path.
