# Verification State

No runtime success is claimed until the pull request passes CI, merges to `main`, the customer-web delivery pipeline deploys that merge commit, and the live logo/address acceptance checks pass.
